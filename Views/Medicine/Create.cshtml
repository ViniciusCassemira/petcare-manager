@model system_petshop.Models.Medicine

@{
    ViewData["Title"] = "Medicine";
}

<h2 class="text-center mb-4">Add Medicine</h2>

<div class="container w-50">
    <form asp-action="Create">
        <div asp-validation-summary="ModelOnly" class="text-danger mb-3"></div>

        <div class="mb-3">
            <label asp-for="Description" class="form-label fw-semibold"></label>
            <input asp-for="Description" class="form-control" />
            <span asp-validation-for="Description" class="text-danger"></span>
        </div>

        <div class="mb-4">
            <label asp-for="Value" class="form-label fw-semibold"></label>
            <input asp-for="Value" class="form-control" step="0.01" type="number" />
            <span asp-validation-for="Value" class="text-danger"></span>
        </div>

        <div class="d-flex gap-2">
            <input type="submit" value="Add" class="btn btn-primary w-50" />
            <a asp-action="Index" class="btn btn-warning w-50">Back to List</a>
        </div>
    </form>
</div>

@section Scripts {
    @{
        await Html.RenderPartialAsync("_ValidationScriptsPartial");
    }
}
